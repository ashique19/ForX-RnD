pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
yfinance>=0.2.40
pyyaml>=6.0
matplotlib>=3.7
joblib>=1.3
streamlit>=1.32
plotly>=5.18
# Optional extras — not required. The lab ships a causal TA subset in
# forex_lab/ta_pack.py (backend: native) so CI does not need numba.
# `pip install pandas-ta` enables feature_extras.pandas_ta.backend: pandas_ta.
# FRED series use public CSV (no key). `pip install fredapi` is used only when
# FRED_API_KEY is set.
# pandas-ta>=0.4.71b0
# fredapi>=0.5.1

# Decision API (Vite desk). Streamlit does not import these.
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
